import { createContext } from 'react';

export const CurrentStateContext = createContext(null);
export const ScheduleContext = createContext(null);
export const ScheduleDispatchContext = createContext(null);
export const SelectorContext = createContext(null);
export const SelectorDispatchContext = createContext(null);
